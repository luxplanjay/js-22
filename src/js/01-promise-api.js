/*
 * Создание промиса
 *  - Класс Promise
 *  - resolve
 *  - reject
 *  - Promise.prototype.then(onResolve, onReject)
 */

//  'Промис выполнился успешно, с результатом (исполнен, fulfilled)'
//  'Промис выполнился с ошибкой (отклонён, rejected)'
//  console.log(`✅ ${result}`);
//  console.log(`❌ ${error}`);

/*
 * Цепочки промисов (chaining)
 */

/*
 * Promise.prototype.catch(error)
 */

/*
 * Promise.prototype.finally()
 */
